keys:
  - &example age10tn8mslgvx3cg58guvqz88eee7qjglkycq79t3jxee0gdc5mjefq2eqrmd
creation_rules:
  # Matched relative to this file's own directory (sops walks up from the
  # target file looking for the nearest .sops.yaml) -- every user's
  # <username>.yaml lives directly alongside this file.
  - path_regex: ^[^/]+\.(yaml|json|env|ini)$
    key_groups:
      - age:
        - *example
