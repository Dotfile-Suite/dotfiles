Add things that need to be done but don't completely pertain to the current topic. You can remove TODOs as they are done:

- `pkgs/dfs-sops/default.nix` has placeholder `lib.fakeHash` for both `src.hash` and `vendorHash` -- there's no `nix` binary in this sandbox to compute the real ones. On a machine with Nix: run `nix build .#myPkgs.x86_64-linux.dfs-sops` twice (once per hash mismatch it reports) and paste each real hash in.
- `dfs-sops` isn't wired in as the system's actual sops yet -- it's just built and available. Two integration points to do once the hashes above are real and it's confirmed to build: (1) point sops-nix's package option at it instead of the nixpkgs default (need to confirm the exact sops-nix option name), (2) update `hosts/common/secrets/README.md`'s manual-fallback instructions to suggest it too.
- Dotfile-Suite/sops's own CI has a real, root-caused, unfixed failure: the "CLI" workflow's functional-tests job fails almost entirely because its test fixture (`functional-tests/.sops.yaml`) has no age-based creation rule, only PGP ones, and PGP is fully disabled in this fork. Needs a live `sops` binary and iterative `cargo test` runs to fix properly (generate a fixture age key, migrate the rules, ignore tests that inherently need a removed backend) -- see [Dotfile-Suite/sops#1](https://github.com/Dotfile-Suite/sops/pull/1) and that repo's `dfs/README.md` "Known failure" section for the precise diagnosis and next steps.
- Dotfile-Suite/sops's fork now goes through PRs (branch-protection requires it; the very first strip-down commit bypassed that rule to establish the fork's baseline, everything since has gone through a PR).
- The "all core deps get a dfs- fork" goal (see REDESIGN.md) only has one dependency (sops) actually done. Dotfile-Suite/template's dfs-fork-sync reusable workflow is written generically, but it's only been proven against this one case so far.
